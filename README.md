# docker-pymanoid
## Instllation & Set up

1. Install nvidia-docker2 & docker-compose
2. Build docker

 ```
 docker-compose build
 ```

## How to use

 ```
 docker-compose run pymanoid bash
 ```
